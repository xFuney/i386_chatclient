'use strict';

// Return versioning information to bootstrapping.

// TODO: Maybe move this to a JSON file instead of bodging it in like this?
module.exports = {
    version: "0.0.2",
    branch: "main" // main, dev, alpha are valid branches.
}